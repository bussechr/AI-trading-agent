# MCP Release Registry

Read-only registry and release inspection surface.

Primary sources:
- `fx-quant-stack/artifacts/active_models.json`
- `fx-quant-stack/artifacts/registry`
- `fx-quant-stack/artifacts/releases`

This service may inspect release candidates, but it does not activate or mutate them.
