# MCP Runtime State

Read-only runtime inspection surface.

Sources:
- `/v2/ready`
- `/v2/state`
- `/v2/decision-snapshots`
- `/v2/orchestration/runs`
- `/v2/orchestration/traces`

This service does not expose queue mutation or execution commands.
