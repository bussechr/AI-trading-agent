<UserControl x:Class="TradingAgent.Dashboard.App.Views.IndicatorMirrorView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">
    <Grid Margin="6">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0"
                   Style="{StaticResource PaneTitleText}"
                   Text="Indicator Mirror (/v2/visuals/tap)"
                   Margin="0,0,0,6" />

        <DataGrid Grid.Row="1"
                  AutoGenerateColumns="False"
                  ItemsSource="{Binding VisualEvents}">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Time" Binding="{Binding TimeUtc, StringFormat=HH:mm:ss}" Width="*" />
                <DataGridTextColumn Header="Symbol" Binding="{Binding Symbol}" Width="*" />
                <DataGridTextColumn Header="Type" Binding="{Binding Type}" Width="*" />
                <DataGridTextColumn Header="Side" Binding="{Binding Side}" Width="*" />
                <DataGridTextColumn Header="Price" Binding="{Binding Price, StringFormat=F5}" Width="*" />
                <DataGridTextColumn Header="Text" Binding="{Binding Text}" Width="3*" />
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
