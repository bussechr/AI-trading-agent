<UserControl x:Class="TradingAgent.Dashboard.App.Views.EquityDrawdownView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:lvc="http://schemas.livecharts.com/2.0/wpf"
             mc:Ignorable="d">
    <Grid Margin="6">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Style="{StaticResource PaneTitleText}" Text="Equity / Drawdown (24h)" Margin="0,0,0,6" />

        <Border Grid.Row="1" Margin="0,0,0,8" Background="{DynamicResource BrushPanelAlt}">
            <lvc:CartesianChart Series="{Binding EquitySeries}"
                                XAxes="{Binding TimeAxis}"
                                YAxes="{Binding ValueAxis}" />
        </Border>

        <Border Grid.Row="2" Background="{DynamicResource BrushPanelAlt}">
            <lvc:CartesianChart Series="{Binding DrawdownSeries}"
                                XAxes="{Binding TimeAxis}"
                                YAxes="{Binding ValueAxis}" />
        </Border>
    </Grid>
</UserControl>
