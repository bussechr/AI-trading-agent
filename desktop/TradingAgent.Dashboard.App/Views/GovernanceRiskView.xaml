<UserControl x:Class="TradingAgent.Dashboard.App.Views.GovernanceRiskView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">
    <Grid Margin="6">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Style="{StaticResource PaneTitleText}" Text="Governance + Risk Envelope" Margin="0,0,0,6" />

        <Grid Grid.Row="1" Margin="0,0,0,8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" Margin="0,0,8,0">
                <StackPanel>
                    <TextBlock Style="{StaticResource MutedText}" Text="Regime" />
                    <TextBlock Style="{StaticResource MetricValueText}" Text="{Binding RiskRegime}" />
                </StackPanel>
            </Border>

            <Border Grid.Column="1" Margin="0,0,8,0">
                <StackPanel>
                    <TextBlock Style="{StaticResource MutedText}" Text="Soft DD" />
                    <TextBlock Style="{StaticResource MetricValueText}" Text="{Binding SoftDdPct, StringFormat=P2}" />
                </StackPanel>
            </Border>

            <Border Grid.Column="2" Margin="0,0,8,0">
                <StackPanel>
                    <TextBlock Style="{StaticResource MutedText}" Text="Hard DD" />
                    <TextBlock Style="{StaticResource MetricValueText}" Text="{Binding HardDdPct, StringFormat=P2}" />
                </StackPanel>
            </Border>

            <Border Grid.Column="3">
                <StackPanel>
                    <TextBlock Style="{StaticResource MutedText}" Text="Daily Breaker" />
                    <TextBlock Style="{StaticResource MetricValueText}" Text="{Binding DailyBreakerPct, StringFormat=P2}" />
                </StackPanel>
            </Border>
        </Grid>

        <DataGrid Grid.Row="2"
                  AutoGenerateColumns="False"
                  ItemsSource="{Binding GovernanceEvents}">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Time" Binding="{Binding TimeUtc, StringFormat=HH:mm:ss}" Width="*" />
                <DataGridTextColumn Header="Type" Binding="{Binding EventType}" Width="*" />
                <DataGridTextColumn Header="Reason" Binding="{Binding Reason}" Width="2*" />
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
