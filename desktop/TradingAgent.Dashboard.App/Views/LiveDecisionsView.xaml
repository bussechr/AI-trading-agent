<UserControl x:Class="TradingAgent.Dashboard.App.Views.LiveDecisionsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">
    <Grid Margin="6">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0"
                   Style="{StaticResource PaneTitleText}"
                   Text="Live Decisions"
                   Margin="0,0,0,6" />

        <DataGrid Grid.Row="1"
                  AutoGenerateColumns="False"
                  ItemsSource="{Binding LiveDecisions}">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Symbol" Binding="{Binding Symbol}" Width="*" />
                <DataGridTextColumn Header="Side" Binding="{Binding Side}" Width="*" />
                <DataGridTextColumn Header="Score" Binding="{Binding Score, StringFormat=F3}" Width="*" />
                <DataGridTextColumn Header="Confidence" Binding="{Binding Confidence, StringFormat=F1}" Width="*" />
                <DataGridTextColumn Header="Price" Binding="{Binding Price, StringFormat=F5}" Width="*" />
                <DataGridTextColumn Header="Target %" Binding="{Binding TargetPct, StringFormat=P2}" Width="*" />
                <DataGridTextColumn Header="Reason" Binding="{Binding Reason}" Width="2*" />
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
